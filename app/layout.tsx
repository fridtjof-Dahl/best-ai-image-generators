export const metadata = {
  title: "Best AI Image Generators 2025",
  description: "Compare the best AI image generators",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
